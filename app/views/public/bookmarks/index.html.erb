<div class="container">
  <div class="row">
    <div class="col-md-12">
        <h2>
          <i class="far fa-bookmark"></i>\\Bookmark List!!//<i class="far fa-bookmark"></i>
        </h2>
        <table>
          <thead>
            <tr>
              <th class="custom-header"></th>
              <th class="custom-header"></th>
              <th class="custom-header">商品名</th>
              <th class="custom-header">掲載の有無</th>
              <th class="custom-header"></th>
              <th class="custom-header"></th>
            </tr>
            </thead>
              <tbody>
                <% @bookmarks.each do |bookmark| %>
                  <tr>
                    <td>
                        <% if @current_time < bookmark.created_at.since(7.days) %>
                          <div class = "new-mark">
                            <%#= bookmark.image_tag("new.png", alt: "new", width: "40", height: "40") %></div>
                        <% end %>
                    </td>
                    <td><%= bookmark.review.title %></td>
                    <td>
                        <p><%= link_to review_path(bookmark.review_id), class: "text-decoration-none text-dark" do %>
                        show
                        <% end %></p>
                    </td>
                  </tr>
                <% end %>
              </tbody>
          </table>
    </div>
  </div>
</div>