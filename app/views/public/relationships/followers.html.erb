 <p id="notice"></p>
 <div class="container">
   <div class="row">
    <h2>\\Follower!!//<i class="fas fa-user-plus"></i></h2>
        <table class ='table'>
          <thead>
            <tr>
              <th></th>
              <th><h6>\\Nick Name//</h6></th>
              <th></th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% if @members.exists? %>
            <% @members.each do |member| %>
            <tr>
              <td> <%= image_tag member.get_profile_image(50,50) %></td>
              <td><%= member.nick_name %></td>
              <td>フォロー数: <%= member.followings.count %></td>
              <td>フォロワー数: <%= member.followers.count %></td>
              <td><%= link_to "Show",mypage_path(current_member) %></td>
            </tr>
            <% end %>
          </tbody>
            <% else %>
              <p>ユーザーはいません</p>
            <% end %>
        </table>
    <div class="text-right mt-3">
      <%= link_to followings_path(member_id: current_member.id) do %>
        <h3>\\Follow!!//</h3>
        <h6>Click here for follow list!!!</h6>
      <% end %>
    </div>
    
    
   </div>
 </div>